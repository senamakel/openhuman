import type { TranslationMap } from './types';

// French (Français) translations. Missing keys fall back to English via
// I18nContext.resolveEn().
const fr: TranslationMap = {};

export default fr;
