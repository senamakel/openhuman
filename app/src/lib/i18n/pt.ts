import type { TranslationMap } from './types';

// Portuguese (Português) translations. Missing keys fall back to English via
// I18nContext.resolveEn().
const pt: TranslationMap = {};

export default pt;
