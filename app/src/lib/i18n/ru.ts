import type { TranslationMap } from './types';

// Russian (Русский) translations. Missing keys fall back to English via
// I18nContext.resolveEn().
const ru: TranslationMap = {};

export default ru;
