import type { TranslationMap } from './types';

// Spanish (Español) translations. Missing keys fall back to English via
// I18nContext.resolveEn().
const es: TranslationMap = {};

export default es;
