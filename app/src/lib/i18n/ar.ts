import type { TranslationMap } from './types';

// Modern Standard Arabic (العربية) translations. Missing keys fall back to
// English via I18nContext.resolveEn(). RTL layout handled in I18nProvider.
const ar: TranslationMap = {};

export default ar;
